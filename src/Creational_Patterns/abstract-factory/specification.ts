export type monitor = {
    screensize: number;
    injection_rate: string;
    resolution: number;
    brightness: number;
    panel: string;
}
export type mouse = {
    reactionRate: string; 
    transmissionSystem: string; 
    numberOfButtons: string; 
    connectionSystem: string;
}
export type desktop = {
    cpu: string;
    core: string;
    graphic: string;
    speed: string;
}
